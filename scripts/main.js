


let myHeading = document.querySelector("h1");
myHeading.textContent = "Hello world!";


d = RobotDevice(
    product_id=device.product_id,
    device_id=device.device_id,
    env=device.env.value,
    type=device.type.value,
    description=device.description,
)